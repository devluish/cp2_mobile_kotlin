<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cp2_mobile"
        tools:targetApi="31">

        <!-- Declaração da atividade TelefoneActivity -->
        <activity
            android:name=".TelefoneActivity"
            android:exported="false" />

        <!-- Declaração da atividade CalculadoraActivity -->
        <activity
            android:name=".CalculadoraActivity"
            android:exported="false" />

        <!-- Declaração da atividade HomeActivity (atividade inicial) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
